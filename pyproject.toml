[project]
name = "task-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.18.0",
    "asyncpg>=0.31.0",
    "fastapi>=0.128.0",
    "httpx>=0.28.1",
    "pydantic[email]>=2.12.5",
    "pydantic-settings>=2.12.0",
    "sqlalchemy>=2.0.45",
    "uvicorn[standard]>=0.40.0",
    "pyjwt>=2.10.1",
    "python-multipart>=0.0.21",
    "pwdlib[argon2]>=0.3.0",
    "slowapi>=0.1.9",
]

[tool.pyrefly]
project-includes = [
    "**/*.py*",
    "**/*.ipynb",
]
project-excludes= [
    "app/migrations/versions/*.py"
]

[tool.ruff]
extend-exclude = ["app/migrations/versions/*.py"]

[tool.ruff.lint]
select = [
    "F",        # Pyflakes rules
    "W",        # PyCodeStyle warnings
    "E",        # PyCodeStyle errors
    "I",        # Sort imports properly
    "UP",       # Warn if certain things can changed due to newer Python versions
    "C4",       # Catch incorrect use of comprehensions, dict, list, etc
    "FA",       # Enforce from __future__ import annotations
    "ISC",      # Good use of string concatenation
    "ICN",      # Use common import conventions
    "SIM",      # Common simplification rules
    "TID",      # Some good import practices
    "PTH",      # Use pathlib instead of os.path
    "NPY",      # Some numpy-specific things
    "RUF",      # Ruff-specific rules
    "ARG001",   # unused arguments in functions
    "ANN",      # flake8-annotations (Strict type hinting)
    "B",        # flake8-bugbear (Common logic errors)
    "FAST",     # FastAPI
    "ASYNC",    # flake8-async (crucial for FastAPI/Async SQLAlchemy)
]

[dependency-groups]
dev = [
    "pyrefly>=0.51.0",
    "ruff>=0.14.10",
]
